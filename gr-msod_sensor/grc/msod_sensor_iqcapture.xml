<?xml version="1.0"?>
<block>
  <name>iqcapture</name>
  <key>msod_sensor_iqcapture</key>
  <category>msod_sensor</category>
  <import>import msod_sensor</import>
  <make>msod_sensor.iqcapture($itemsize)</make>
  <param>
    <name>Item size</name>
    <key>itemsize</key>
    <type>int</type>
  </param>

  <!-- Make one 'sink' node per input. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <sink>
    <name>in</name>
    <type>float</type>
  </sink>

  <!-- Make one 'source' node per output. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <source>
    <name>out</name>
    <type>float</type>
  </source>
</block>
